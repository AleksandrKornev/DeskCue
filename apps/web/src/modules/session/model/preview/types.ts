export type PreviewTicketState = {
  documentRevision: number;
  error: string;
  key: string;
  loading: boolean;
  resolvedCredentialRevision: string;
  resolvedKey: string;
  url: string | null;
};
